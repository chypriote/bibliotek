{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "lint": "eslint --fix --ext .js,.vue,.ts --ignore-path .gitignore .",
    "stylelint": "stylelint --fix --ignore-path .gitignore **/*.{css,vue}",
    "release": "standard-version"
  },
  "lint-staged": {
    "*.{js,vue,ts}": "eslint --cache --fix",
    "*.{css,vue}": "stylelint --fix"
  },
  "dependencies": {
    "date-fns": "^2.29"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.3",
    "@commitlint/config-conventional": "^17.3",
    "@nuxtjs/eslint-config-typescript": "^12.0",
    "@nuxtjs/eslint-module": "^3.1",
    "@nuxtjs/stylelint-module": "^4.1",
    "eslint": "^8.30",
    "eslint-plugin-vue": "^9.8",
    "husky": "^8.0",
    "lint-staged": "^13.1",
    "nuxt": "^3.0",
    "postcss-html": "^1.5.0",
    "postcss-import": "^15.1",
    "postcss-nested": "^6.0",
    "reset-css": "^5.0",
    "stylelint": "^14.16",
    "stylelint-config-standard": "^29.0"
  },
  "peerDependencies": {
    "postcss": "^8.4"
  }
}
